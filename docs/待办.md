# 待办事项

> 更新于 2026-07-10

## 未修复 Bug

| ID | 位置 | 问题 | 严重度 |
|------|------|------|:--:|
| B11 | `realtime/fusion_router.py` L175 | YOLO 3D 定位 depth=3.0 硬编码，需接入真实深度图 | 中 |
| B13 | `fusion/manager.py` L140-144 | wheel_base 未传入 StateEstimator，始终用固定 1.5m | 中 |
| B14 | `state_estimation/estimator.py` `get_position()` | _history 乱序包导致插值错误 | 中 |
| B16 | `reconstruction/projector.py` | project_pixel_defect 未考虑镜头畸变 | 中 |
| B17 | `reconstruction/routes.py` L217-227 | WebSocket _ws_clients 无锁，并发可能竞态 | 中 |

## 未修复前端 Bug

| ID | 位置 | 问题 | 严重度 |
|------|------|------|:--:|
| B26 | `RealtimeView.vue` L248-259 | 组件卸载时 ws.close() 触发 onclose 自动重连 | 中 |
| B31 | `ConnectionBar.vue` L9 | v-model.number 输入非数字变 NaN | 中 |
| B35 | `DetectionView.vue` L50-56 | URL.createObjectURL 未 revoke | 中 |
| B38 | `RealtimeView.vue` L103-104 | seenLoc/seenDet Set 无界增长 | 中 |

## 设计改进（待评估）

| ID | 建议 |
|------|------|
| D1 | 引入 PipelineService 依赖注入 |
| D3 | 全局单例 → app.state 或文档标明单 worker |
| D5 | 配置集中到 config.py + pydantic-settings |
| D10 | 前端 Pinia 状态管理 |
| D11 | 统一 API 层（减少裸 fetch） |
| D12 | WebSocket 管理抽象为 composable |
| D13 | ReconstructionViewer 拆分 (Three.js 拆分) |
| D18 | 路由懒加载 + 移除 Page2/3/4 占位页 |

## 性能优化（长期）

| ID | 方向 |
|------|------|
| P1 | Poisson 重建阻塞 → 专用重建队列 / TSDF 增量融合 |
| P3 | 逐点 sample_bilinear → cv2.remap 批量采样 |
| P4 | 灰色点 O(M×K) → scipy KDTree 批量 query |
| P5 | get_position 线性搜索 O(n) → bisect 二分查找 O(log n) |
| P9 | 前端每帧 render → dirty flag 按需渲染 |
| P11 | 裂缝独立 SphereGeometry → InstancedMesh 复用 |
