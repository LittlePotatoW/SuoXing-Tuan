# 项目架构与文件夹结构设计

## 项目背景
我们要开发一个**工业检测平台**，核心功能是实时接收硬件通过网络传输的图像/激光雷达数据，通过大模型进行缺陷识别，并结合三维重建在前端可视化展示检测结果。同时支持加载本地文件数据包进行离线测试与回放。

## 技术栈
- **前端**: Electron + Vue 3（桌面应用）
- **后端**: Python（硬件通信、数据处理、AI推理）
- **模型训练**: Python + PyTorch（缺陷检测模型、多模态模型的训练与评估）
- **模型推理**: ONNX Runtime / TensorRT（模型部署与推理加速）

## 核心功能模块

### 1. 前端（Electron + Vue 3）
- 三维重建结果的可视化渲染（高斯泼溅 / Gaussian Splatting）
- 实时图像和识别结果的同步显示
- 缺陷位置在三维模型上的标注与交互
- 硬件数据流的状态监控面板

### 2. 后端（Python）
- **硬件通信层**：与工业相机、激光雷达等设备通信，接收图像流和点云数据
- **数据处理层**：图像预处理、点云配准与融合
- **三维重建引擎**：基于高斯泼溅（3D Gaussian Splatting）进行场景重建
- **AI推理层**：加载训练好的大模型，提供缺陷检测与图像识别推理服务
- **API 网关层**：统一的 RESTful / WebSocket 接口供前端调用

### 3. 模型训练模块（独立于前后端）
- **数据管理**：训练数据集的采集、标注、清洗、版本管理
- **模型训练**：缺陷检测模型的训练脚本、超参配置、训练日志
- **模型评估**：精度评估、推理速度基准测试、模型对比报告
- **模型导出**：训练好的模型导出为 ONNX/TensorRT 格式，供后端推理层加载
- **模型仓库**：已训练模型的版本管理、模型文件存储与分发

## 版本迭代规划

| 版本 | 目标 | 核心交付 |
|------|------|----------|
| V1 | 基于图像的识别 | 图像采集 → 大模型缺陷检测 → 前端展示识别结果 |
| V2 | 多模态三维重建 | 激光雷达+图像融合 → 3D Gaussian Splatting 重建 → 前端三维渲染 |
| V3 | 三维标注系统 | 大模型从图像中识别缺陷，自动将识别结果映射并标注到三维重建场景中的对应位置 |

## 架构设计要求

### 必须满足
1. **前后端完全解耦**：前端仅通过 API/WebSocket 与后端通信，不直接依赖任何硬件或模型
2. **大模型可插拔**：AI推理层抽象统一接口，支持快速切换不同模型（如 YOLO → Transformer → 多模态大模型），切换模型不应影响其他模块
3. **后端模块高度解耦**：各模块（硬件通信、三维重建、AI推理）独立部署、独立扩展，通过消息队列或 gRPC 通信
4. **数据流可追溯**：每个处理环节记录输入输出，便于调试和复现

### 建议考量
- 实时性：图像识别延迟 < 200ms（V1），三维重建刷新率 ≥ 10fps（V2）
- 可扩展性：后续可接入更多传感器类型
- 离线模式：模型推理支持本地部署，不依赖云端

## 输出要求

请生成以下 **两个 .md 文件**：

### 文件 1：`ARCHITECTURE.md`
- 系统整体架构图（用 Mermaid 或 ASCII 描述）
- 各层职责与边界说明
- 数据流说明（从硬件采集 → 处理 → 前端展示的完整链路）
- 模块间通信方式（REST、WebSocket、gRPC、消息队列的选择与理由）
- 大模型替换的热插拔设计
- 跨版本演进策略（V1→V2→V3 如何渐进式扩展）

### 文件 2：`PROJECT_STRUCTURE.md`
- 完整的项目文件夹树形结构，至少包含以下顶层目录：
  - `frontend/`：Electron + Vue 3 前端代码
  - `backend/`：Python 后端（按功能模块进一步拆分：硬件通信、数据处理、三维重建、AI推理、API网关）
  - `model-training/`：大模型训练完整代码（数据预处理、训练脚本、评估脚本、配置文件、模型导出），与前后端完全分离
  - `shared/` 或 `common/`：前后端共享的类型定义、通信协议定义（如 Protobuf schema、JSON Schema）
  - `scripts/`：项目级部署、构建、环境初始化、数据处理脚本
  - `docs/`：项目文档目录（含生成的 ARCHITECTURE.md 和 PROJECT_STRUCTURE.md）
- 每个目录的用途说明（README 式的一两句话）
- 关键文件的职责说明

## 补充说明
- 三维重建初期使用 3D Gaussian Splatting，但架构上保留未来替换为 NeRF 等其他方案的可能性
- 缺陷类型、硬件型号等暂时不限定，设计中保持通用性
- 前后端通信协议建议优先使用 Protocol Buffers 或 JSON Schema 做类型约束