# 重建引擎

一个类，用于持续接收重建帧，并最后输出重建结果

## 必须实现的方法
- stop()：停止并释放重建引擎实例
- create()：创建重建引擎实例（有参数，比如多少帧触发重建，是全量重建还是增量重建）
等等，你帮我继续设计

## 注意！
这个设计成单例
你需要帮我完善，基于我们的设计，输入对齐的激光雷达和图像数据，输出更为精确的结果

## 配置
配置文件将会至少包含以下内容，供重建引擎使用（如果有硬性需要的，你可以继续要求我提供）：
相机的内参矩阵
激光雷达和相机的位姿
重建涉及的参数，你帮我设计
