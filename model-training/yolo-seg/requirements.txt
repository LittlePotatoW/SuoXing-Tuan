# ============================================================
# tunnel-defect-ml/requirements.txt
# 隧道缺陷检测模型训练依赖
#
# 用法:
#   pip install -r requirements.txt
# ============================================================

# 核心框架
ultralytics>=8.2.0           # YOLOv8-seg 实例分割
torch>=2.0.0                 # PyTorch (CUDA 版本需手动装)
torchvision>=0.15.0

# 数据处理
numpy>=1.24.0
opencv-python>=4.8.0
pandas>=2.0.0
pillow>=10.0.0

# 标注解析与配置
pyyaml>=6.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# 进度条
tqdm>=4.65.0

# Jupyter (可选)
ipython>=8.0.0
jupyterlab>=4.0.0
ipywidgets>=8.0.0

# ONNX 导出 (可选)
onnx>=1.14.0
onnxruntime-gpu>=1.16.0      # GPU 推理加速
